\begin{tikzpicture}
  \begin{axis}[
      % title=Quantum Mechanical Wave Function,
      hide axis,
      colormap/cool,
    ]
    \addplot3[
      mesh,
      samples=50,
      domain=-8:8,
    ]
    {sin(deg(sqrt(x^2+y^2)))/sqrt(x^2+y^2)};
    % \addlegendentry{\(\frac{sin(r)}{r}\)}
  \end{axis}
\end{tikzpicture}
